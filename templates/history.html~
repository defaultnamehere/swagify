{% extends 'base.html'  %}
{% block container %}

<div class="page-header">

    <h1> {{ name }}  <small> History </small> </h1>
</div>

    {% for event in events%}
    <div class="event-{{ event.commit_hash }} well well-small" >
        <p class="lead">
            {% if event.delta_karma > 0 %}
            <span class="badge badge-success" style="vertical-align: middle;" > +{{ event.delta_karma }}</span> karma for <span style="vertical-align: middle;" class="label label-info"> {{ event.reason }} </span>
            {% else %}
            <span style="vertical-align: middle;" class="badge badge-important"> {{ event.delta_karma }}</span> karma for <span style="vertical-align: middle;" class="badge badge-info"> {{ event.reason }} </span>

            {% endif %}
        </p>
        <div class="details">
        <p class="nope"><span class="muted" >Commit {{ event.commit_hash }} | {{ event.date }}</span> </p>
        </div>
    </div>
    {% endfor %}

    {% if not events %}
    <div class="well well-small">
        <span class="leader muted"> No history for {{ name }}. </span>
    </div> 
    <p class="muted leader"> Keep up the good work! </p>

    {% endif %}
    <br/>
    <br/>
    <br/>
    <br/>
    <br/>
    <br/>
    <br/>
    <br/>
    <br/>
    <br/>
    <br/>
    <blockquote class="pull-right ">
        <p> My karma ran over your dogma.</p>
        <small> Rick Astley </small>
    </blockquote>
{% endblock %}
{% block includejs %}

{% endblock %}
